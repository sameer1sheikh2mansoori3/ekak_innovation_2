import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'media.istockphoto.com' , 'home.ekak.in' , 'media.licdn.com'],
  },
};

export default nextConfig;
