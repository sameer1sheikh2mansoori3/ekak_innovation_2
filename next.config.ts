import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'wallpapers.com', 'i.all3dp.com', 'images.pexels.com', 'media.istockphoto.com' , 'home.ekak.in' , 'media.licdn.com' , 'plus.unsplash.com'],
  },
};

export default nextConfig;
